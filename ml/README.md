# ML Models

This directory contains model training scripts and evaluation harnesses.

## Structure

- `train/` - Training scripts for claim worthiness classifier
- `eval/` - Evaluation harness for pipeline metrics
- `models/` - Trained model artifacts (gitignored)
- `data/` - Training and evaluation datasets (gitignored)
